#include<bits/stdc++.h>
using namespace std;
class Solution {
public:
bool range(int n, int i){
    return (i>=0 && i<n);
}
vector<int> searchRange(vector<int>& nums, int k) {
    int n=nums.size();
    int leftIdx=0;
    int left=0, right=n-1;
    while(left<=right){
        int mid=left-((right-left)/2);
        
    }
}
};
int main()
{

    return 0;
}